---
description: Show current ALBA user info and token balance
allowed-tools: Bash
---

Display the currently authenticated ALBA user's information and token balance.

Command: cd ${CLAUDE_PLUGIN_ROOT} && npm install --silent 2>/dev/null && node scripts/whoami.mjs
