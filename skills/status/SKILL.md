---
description: Check ALBA backend connection and health status
allowed-tools: Bash
---

# ALBA Status

Check the ALBA backend health and connection status.

## Instructions

Execute the following command exactly — do not add commentary before or after:

```bash
cd ${CLAUDE_PLUGIN_ROOT} && npm install --silent 2>/dev/null && node scripts/status.mjs
```
