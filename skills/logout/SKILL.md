---
description: Log out of ALBA — removes saved credentials
allowed-tools: Bash
---

Log out from ALBA and remove saved credentials from ~/.alba/credentials.json.

Command: cd ${CLAUDE_PLUGIN_ROOT} && npm install --silent 2>/dev/null && node scripts/logout.mjs
