---
description: Log in to ALBA via Google SSO — opens browser for authentication
allowed-tools: Bash
---

Run the ALBA login flow. This opens a browser for Google authentication and saves credentials locally.

Command: cd ${CLAUDE_PLUGIN_ROOT} && npm install --silent 2>/dev/null && node scripts/login.mjs
