---
description: List available projects in the ALBA marketplace
allowed-tools: Bash
---

# ALBA Projects

Fetch and display available projects from the ALBA marketplace.

## Instructions

Execute the following command exactly — do not add commentary before or after:

```bash
cd ${CLAUDE_PLUGIN_ROOT} && npm install --silent 2>/dev/null && node scripts/projects.mjs
```
