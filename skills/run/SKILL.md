---
description: Start ALBA — auto-login + 5-phase AI project build simulation
allowed-tools: Bash
---

# ALBA Run

Start the ALBA agent. Automatically logs in if needed, then begins building AI micro-MVPs.

## Instructions

Execute the following command exactly — do not add commentary before or after:

```bash
cd ${CLAUDE_PLUGIN_ROOT} && npm install --silent 2>/dev/null && node scripts/run.mjs
```
